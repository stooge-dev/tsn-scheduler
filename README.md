# tsn-scheduler
Online re-scheduling of TSN 802.1Qbv GCL while being time-bound.

## Usage
The project is build via cmake.
First generate the configuration:
`cmake --preset default`

Then build the project via:
`cmake --build ./build`